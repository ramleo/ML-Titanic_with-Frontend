# ML Pipeline Summary

_Generated by auto_pipeline.py on 2026-05-28 00:42:42_

---

## 1. Dataset Overview

| Property | Value |
|---|---|
| File | Titanic-Dataset.csv |
| Rows | 891 |
| Columns | 12 |
| Target Column | `Survived` |
| Task Type | **Classification** |
| Unique Target Values | 2 |
| Deployment Platform | render |

### Missing Values
| column | missing | pct |
| --- | --- | --- |
| Age | 177.0 | 19.9 |
| Cabin | 687.0 | 77.1 |
| Embarked | 2.0 | 0.2 |

---

## 2. Features Used

**Numeric (6):** `PassengerId`, `Pclass`, `Age`, `SibSp`, `Parch`, `Fare`

**Categorical (4):** `Name`, `Sex`, `Ticket`, `Embarked`

**Dropped (>50% missing):** `Cabin`

---

## 3. Preprocessing Pipeline

| Step | Detail |
|---|---|
| Missing (numeric) | SimpleImputer(strategy="median") |
| Missing (categorical) | SimpleImputer(strategy="most_frequent") |
| Scaling | StandardScaler |
| Encoding | OneHotEncoder(handle_unknown="ignore") |
| Train/Test Split | 80/20, stratified, random_state=42 |
| Train size | 712 rows |
| Test size | 179 rows |

---

## 4. Model Selection & Hyperparameter Tuning

GridSearchCV(cv=3, n_jobs=-1, scoring="accuracy")

| Model | CV Score | Best Params |
|---|---|---|
| GradientBoosting | 0.8245 | {'learning_rate': 0.1, 'n_estimators': 100} |
| RandomForest | 0.8132 | {'max_depth': None, 'n_estimators': 200} |
| LogisticRegression | 0.8090 | {'C': 10} |

**Winner:** `GradientBoosting` with CV accuracy = **0.8245**

---

## 5. Model Evaluation

| Metric | Value |
|---|---|
| Test Accuracy | 0.7933 |
| CV Score (accuracy) | 0.8245 |

**Classification Report:**
```
              precision    recall  f1-score   support

           0       0.78      0.93      0.85       110
           1       0.83      0.58      0.68        69

    accuracy                           0.79       179
   macro avg       0.81      0.75      0.77       179
weighted avg       0.80      0.79      0.78       179

```

---

## 6. Final Pipeline Architecture

```
CSV Input
    └── ColumnTransformer
            ├── numeric  → SimpleImputer(median) → StandardScaler
            └── categorical → SimpleImputer(most_frequent) → OneHotEncoder
                └── GradientBoosting(learning_rate=0.1, n_estimators=100)
                        └── Prediction
```

---

## 7. Artifacts

| File | Description |
|---|---|
| models/final_pipeline.pkl | Trained sklearn Pipeline (preprocessor + model) |
| models/label_encoder.pkl | Fitted LabelEncoder for target classes |
| plots/eda_correlation.png | Feature correlation heatmap |
| plots/eda_target.png | Target variable distribution |


---

## 8. Reproducibility

```python
import joblib, pandas as pd

pipeline = joblib.load("models/final_pipeline.pkl")
df = pd.read_csv("data/Titanic-Dataset.csv")
X = df.drop(columns=["Survived"])
predictions = pipeline.predict(X)
print(predictions)
```
